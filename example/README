
yum install pdns pdns-backend-mysql pdns-backend-sqlite

pip install -r requirements.txt

django-admin.py startproject example
cd example

set settings.py

python manage.py validate

python manage.py syncdb

python manage.py runserver 192.168.0.240:9999

pdns_server --config-dir=`pwd`

